export const STATUS = {
    ERROR: 'error',
    FAIL: 'fail'
};

export const DEFAULT_MESSAGE = 'Internal Server Error';
export const DEFAULT_STATUS_CODE = 500;
export const DEFAULT_STATUS = STATUS.ERROR;
